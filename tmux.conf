# Configure terminal environment.
set -sg default-terminal "screen-256color"
set -asg terminal-overrides "xterm*:Tc,smcup@:rmcup@,bce,screen-256color:Tc"
set -wg xterm-keys on
set -wg mode-keys vi

# Session management.
set -g destroy-unattached on

# Snappy keyboard.
set -sg escape-time 0

# Set scrollback history to 10000 (10k).
set -sg history-limit 10000
set -sg history-file "~/.tmux_history"
set -sg display-time 2500

# Set window and pane index to 1 (0 by default).
set -g base-index 1
set -wg pane-base-index 1

# Capture focus events.
set -sg focus-events on

# Renumber windows sequentially after closing any of them.
set -g renumber-windows on

# Always resize windows to match the active parent window.
set -wg aggressive-resize on

# Visual activity feedback.
set -wg monitor-activity on
set -g visual-activity off

# Customize window titles.
set -g set-titles on
set -g set-titles-string "#W"
set -wg allow-rename on
set -wg automatic-rename on
set -wg automatic-rename-format "#T"

# Customize the status bar.
set -g status on
set -g status-interval 10
set -g status-keys vi
set -g status-position bottom
set -g status-justify left
set -g status-style "bg=#008787,fg=default,bold"
set -g status-left ""
set -g status-left-length 0
set -g status-right " #S "
set -g status-right-length 11
set -g message-style "bg=#af8700,fg=white,bold"

# Customize the window status tabs.
set -wg window-status-activity-style "bold"
set -wg window-status-bell-style "bg=red,fg=white,bold"
set -wg window-status-current-format " #[fg=cyan]#I#[fg=white] ╱ #{=24:window_name} "
set -wg window-status-current-style "bg=black,fg=white,bold"
set -wg window-status-format " #[fg=cyan]#I#[fg=default] ╱ #{=24:window_name} "
set -wg window-status-separator " "

# Customize pane appearance.
set -wg pane-border-style "bg=default,fg=#444444"
set -wg pane-active-border-style "bg=default,fg=#008787"

# Set ` (tic) as the default prefix key combination and unbind C-b to free it up.
unbind C-b
set -g prefix `

# General key bindings.
bind ` send-prefix
bind -T root C-Space command-prompt

# Reload ~/.tmux.conf using PREFIX r.
bind r source-file ~/.tmux.conf \; display " Reloaded!"

# Tab and pane navigation.
bind -T root C-n new-window
bind -T root C-w kill-window
bind -T root -r M-C-Right next-window
bind -T root -r C-M-Left previous-window
bind -T root -r M-Up select-pane -U
bind -T root -r M-Down select-pane -D
bind -T root -r M-Left select-pane -L
bind -T root -r M-Right select-pane -R

# Cursor navigation.
bind -T root C-Up copy-mode -e \; send-keys -X scroll-up
bind -T root C-PageUp copy-mode -e \; send-keys -X page-up
bind -T copy-mode-vi C-Home send-keys -X history-top
bind -T copy-mode-vi C-End send-keys -X history-bottom
bind -T copy-mode-vi Home send-keys -X  start-of-line
bind -T copy-mode-vi End send-keys -X end-of-line
bind -T copy-mode-vi Escape send-keys -X cancel
bind -T copy-mode-vi C-Left send-keys -X previous-word
bind -T copy-mode-vi C-Right send-keys -X next-word-end
bind -T copy-mode-vi Space send-keys -X rectangle-toggle
bind -T copy-mode-vi PageUp send-keys -X page-up
bind -T copy-mode-vi PageDown send-keys -X page-down

# Use PREFIX h to split window horizontally and PREFIX v to split vertically.
bind h split-window -h -c "#{pane_current_path}"
bind v split-window -v -c "#{pane_current_path}"
unbind %

# Set mouse behavior.
set -wg mouse on
bind -T root WheelUpPane if -F -t = "#{mouse_any_flag}" "send-keys -M" "select-pane -t =; if -F -t = \"#{alternate_on}\" \"send-keys Up Up Up\" \"copy-mode -e; send-keys -M\""
bind -T root WheelDownPane if -F -t = "#{mouse_any_flag}" "send-keys -M" "select-pane -t =; if -F -t = \"#{alternate_on}\" \"send-keys Down Down Down\" \"copy-mode -e; send-keys -M\""
bind -T copy-mode-vi WheelUpPane send-keys -X -N3 scroll-up
bind -T copy-mode-vi WheelDownPane send-keys -X -N3 scroll-down
bind -T copy-mode-vi MouseDown1Pane send-keys -X clear-selection
unbind -T copy-mode-vi MouseDragEnd1Pane

# Finally, load any platform-specific configuration.
source-file ~/.tmux.platform.conf

# Now create the default persistent sessions and groups.
new-session -s main
set -t main destroy-unattached off
