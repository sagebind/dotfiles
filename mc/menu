C   Copy full path
    printf "copy\nfile://%d/%f\0" | xclip -i -selection clipboard -t x-special/gnome-copied-files

O   Open in default program
    xdg-open "%f"

A   Open in Atom
    atom "%f"

= t r
V   Open in Vim
    tmux new-window "vim %f"

= t x
E   Execute
    fish -c "%f"
