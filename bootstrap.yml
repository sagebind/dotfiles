- name: Bootstrap development environment
  hosts: localhost
  connection: local
  roles:
    - workstation
    - role: pluto
      when: ansible_facts['product_name'] == 'TUXEDO InfinityBook Max 15 AMD Gen10'
    - role: acquia
      when: ansible_facts['product_name'] == 'MacBookPro18,2'
