- name: Bootstrap development environment
  hosts: localhost
  connection: local
  roles:
    - workstation
    - role: fedora
      when: ansible_pkg_mgr == 'dnf5'
    - role: pluto
      when: ansible_product_name == 'TUXEDO InfinityBook Max 15 AMD Gen10'
